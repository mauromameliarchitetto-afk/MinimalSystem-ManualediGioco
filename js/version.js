/* Versione dell'app. Nella versione web resta null (sempre aggiornata dal
   server); nella build APK la GitHub Action sovrascrive questo file con il
   numero di versione reale, usato per il controllo aggiornamenti. */
const APP_VERSION = null;
